#primer numero es (a) y el segundo numero es (b)
def suma():
    a= float(input("ingrese numero que quiere sumar"))
    b= float(input("ingere el numero por el que lo quiere sumar"))
    suma = a + b 
    return suma